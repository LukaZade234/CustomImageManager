# DigitalOcean App Platform configuration
# Deploy: Connect this repo at cloud.digitalocean.com/apps
# Replace REPO with your GitHub repo (e.g. username/CustomImageManager)
name: imgmanager

# Web service - runs Flask so Add Character API works
services:
  - name: web
    run_command: python app.py
    environment_slug: python
    http_port: 8080
